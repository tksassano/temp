def lineFromPoints(p1, p2):
    m = (p2[1]-p1[1]) / (p2[0]-p1[0])
    b = p2[1] - m * p2[0]
    def evaluate(x):
        return int(m * x + b)
    return evaluate

p1 = (2, 1)
p2 = (5, 10)
points = []

f = lineFromPoints(p1, p2)
points.append(p1)
